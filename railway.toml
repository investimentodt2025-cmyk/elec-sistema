[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn servidor:app --bind 0.0.0.0:$PORT --workers 1"
healthcheckPath = "/api/status"
restartPolicyType = "ON_FAILURE"

[[mounts]]
mountPath = "/data"
